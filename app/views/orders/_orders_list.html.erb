<tr> 
    <td> <%= link_to order.status, order_path(order) %> </td>
    <td> <%= link_to order.restaurant_name, restaurant_path(order.restaurant)%></td>
    <td> $<%= order.total_price %></td>
 </tr>
   
